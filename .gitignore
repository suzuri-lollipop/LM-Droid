# Gradle
.gradle/
**/build/

# Local configuration (contains local SDK path, must not be committed)
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
*.ipr
*.iws
captures/
.externalNativeBuild/
.cxx/

# Kotlin
.kotlin/

# Note: app/schemas/ (Room schema history) is intentionally NOT ignored —
# those JSON files are committed for migration tracking.

# Note: vendored native third-party sources (whisper.cpp, stable-diffusion.cpp,
# bullet3) under app/src/main/cpp/ are intentionally tracked. bullet3 is trimmed
# to src/ + root CMake files (demos/data/examples removed) — see CMakeLists.txt.

# Log/OS files
*.log
.DS_Store
Thumbs.db
